[project]
name = "vlm-showcase"
version = "1.0.0"
description = "VLM Model Capability Showcase"
requires-python = ">=3.10"

[project.scripts]
vlm-demo = "vlm_showcase.demo_runner:main"

[tool.setuptools.packages.find]
include = ["vlm_showcase*"]
exclude = ["ros2_ws*"]
